[project]
name = "cisco3560-gui"
version = "0.1.0"
description = "Desktop GUI for managing a Cisco Catalyst 3560 switch over Telnet or serial console"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.11",
    "netmiko>=4.7",
    "pyserial>=3.5",
]

[tool.uv]
package = false
